{
  "name": "somewhere-now",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "世界中の YouTube ライブカメラを地図から選んで覗く。昼夜ターミネータ付き。",
  "scripts": {
    "dev": "vite",
    "dev:worker": "wrangler dev --test-scheduled",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.worker.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "cams:discover": "node --experimental-strip-types scripts/discover-cams.ts",
    "cams:build": "node --experimental-strip-types scripts/build-cams.ts",
    "cams:search": "node --experimental-strip-types scripts/search-cams.ts"
  },
  "dependencies": {
    "@fontsource/barlow": "^5.3.0",
    "@fontsource/barlow-condensed": "^5.3.0",
    "@fontsource/dm-mono": "^5.3.0",
    "leaflet": "^1.9.4",
    "leaflet.markercluster": "^1.5.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260818.1",
    "@types/leaflet": "^1.9.22",
    "@types/leaflet.markercluster": "^1.5.6",
    "@types/node": "^26.2.0",
    "@vitest/coverage-v8": "^4.1.6",
    "typescript": "7.0.2",
    "vite": "8.2.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.1"
  }
}
